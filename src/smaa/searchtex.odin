package smaa

@(private)
SEARCHTEX_WIDTH :: 64
@(private)
SEARCHTEX_HEIGHT :: 16
@(private)
SEARCHTEX_SIZE :: SEARCHTEX_WIDTH * SEARCHTEX_HEIGHT

@(private)
SEARCHTEX_BYTES: []u8 = #load("textures/searchtex.bin")
