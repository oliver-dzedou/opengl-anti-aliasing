package smaa

@(private)
AREATEX_WIDTH :: 160
@(private)
AREATEX_HEIGHT :: 560
@(private)
AREATEX_SIZE :: AREATEX_WIDTH * AREATEX_HEIGHT

@(private)
AREATEX_BYTES: []u8 = #load("textures/areatex.bin")
